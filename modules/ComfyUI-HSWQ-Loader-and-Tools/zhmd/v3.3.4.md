<table align="center">
  <tr>
    <td align="center" bgcolor="#e5e7eb" width="88" height="36"><a href="https://github.com/ussoewwin/ComfyUI-HSWQ-Loader-and-Tools/releases/tag/v3.3.4"><font color="#4b5563"><b>EN</b></font></a></td>
    <td align="center" bgcolor="#3478ca" width="88" height="36"><font color="#ffffff"><b>中文</b></font></td>
  </tr>
</table>

## ① 问题所在

Z Image / ZIT **ConvRot NVFP4**（以及同一 Comfy parity 在线 act rotate 路径上的 INT8 protect Linear）会在每个模块上保存 Hadamard 矩阵 `module._hswq_nvfp4_parity_H`，并在每次 forward 中复用。

**Distorch** VRAM purge 之后，Method 3 可能清空 / 毒化 tensor storage（`t.data = empty` 一类），而 Python 侧仍保留同一属性引用。`nodes/nvfp4/nvfp4_hadamard.py` 中的**全局** Hadamard cache 在复用前已用 `_tensor_storage_ok` 拒绝毒化张量。`_make_convrot_parity_forward` 的**模块本地**路径则**没有**。它只检查：

- `h.device` / `h.dtype` 与 `input` 是否一致
- `h.numel() == 0`
- CUDA / 非 CUDA 是否不一致
- 以及后来补上的 `storage.nbytes() == 0`

因此，device/dtype 看起来仍匹配的壳（有时 `nbytes` / `numel` 形状在 `_tensor_storage_ok` 下仍应拒绝、但旧判定会放过）可能被复用。在线 act rotate 就会用已死 / 垃圾的 `H` 运行。第 1 次生成看起来正常；purge 后的**第 2 次及之后**开始劣化（噪声 / 画质下降），与 clear 路径注释中的 2nd→3rd→4th 逐步变差一致。

**v3.3.4 之前的错误复用判定**（`forward_parity` 内）：

```python
            h = getattr(self, "_hswq_nvfp4_parity_H", None)
            need_rebuild = True
            if h is not None:
                try:
                    need_rebuild = (
                        h.device != input.device
                        or h.dtype != input.dtype
                        or int(h.numel()) == 0
                        or (bool(input.is_cuda) and not bool(h.is_cuda))
                    )
                    if not need_rebuild:
                        # DistOrch Method 3 may empty storage while device/dtype
                        # still match — rebuild or 2nd gen becomes noise.
                        st = (
                            h.untyped_storage()
                            if hasattr(h, "untyped_storage")
                            else h.storage()
                        )
                        need_rebuild = int(st.nbytes()) == 0
                except Exception:
                    need_rebuild = True
```

---

## ② 新增或修改的文件名

| 路径 | 变更 |
|------|------|
| `nodes/zimage_nvfp4/nvfp4_comfy_parity.py` | 模块本地 `H` 复用改为调用 `_tensor_storage_ok`；相关注释产品拼写改为 **Distorch** |
| `nodes/nvfp4/nvfp4_hadamard.py` | docstring：`DistOrch` → **Distorch**（函数本体未改；全局 cache 原本已在使用） |
| `pyproject.toml` | 版本 `3.3.3` → `3.3.4` |
| `__init__.py` | `__version__` `3.3.3` → `3.3.4` |
| `changelog.md` | 增加 Version **3.3.4** 条目 |
| `zhmd/CHANGELOG.md` | 增加 Version **3.3.4** 条目 |
| `zhmd/v3.3.4.md` | 中文发布说明（本文件） |
| `release/_release_v3.3.4_body_en.md` | 英文 GitHub Release 正文 |

本次修复未新增任何 Python 模块文件。行为修复仅在 `nvfp4_comfy_parity.py`（外加版本 / 文档面）。

---

## ③ 新增或修改的代码全文

### 3.1 `nodes/nvfp4/nvfp4_hadamard.py` — `_tensor_storage_ok`（parity 现与之共用的门控）

```python
def _tensor_storage_ok(t) -> bool:
    """False after Distorch nuclear kill / empty-storage reuse (UAF risk)."""
    if t is None:
        return False
    try:
        if int(getattr(t, "numel", lambda: 0)()) <= 0:
            return False
        st = t.untyped_storage() if hasattr(t, "untyped_storage") else t.storage()
        if int(st.nbytes()) <= 0:
            return False
        # Shape must match a square Hadamard (or 4x4 h4); reject emptied shells.
        if getattr(t, "ndim", 0) == 2:
            if int(t.shape[0]) != int(t.shape[1]) or int(t.shape[0]) < 4:
                return False
        return True
    except Exception:
        return False
```

### 3.2 `nodes/nvfp4/nvfp4_hadamard.py` — `clear_hadamard_global_caches` docstring（仅拼写）

```python
def clear_hadamard_global_caches() -> int:
    """Drop module-level Hadamard caches (Distorch Method 2c / parity clear).

    Method 3 may ``t.data = empty`` on tensors still referenced by these dicts.
    Returning them on the next gen rotates with dead/garbage ``H`` and quality
    decays as CUDA reallocates the freed region (2nd→3rd→4th gen worse).
    """
    n = len(_HADAMARD_CACHE) + len(_H4_CACHE)
    _HADAMARD_CACHE.clear()
    _H4_CACHE.clear()
    return n
```

### 3.3 `nodes/zimage_nvfp4/nvfp4_comfy_parity.py` — 修复后的 `_make_convrot_parity_forward` 全文

```python
def _make_convrot_parity_forward(stock_forward):
    """Stock MixedPrecision forward + online act rotate for ConvRot Linears.

    NVFP4: ``_hswq_nvfp4_convrot`` (Params.convrot cleared at load).
    INT8 protect: ``_hswq_int8_convrot`` (Params.convrot cleared at load —
    same as Conv2d). Kitchen must **not** see Params.convrot=True or
    int8_linear double-rotates with this path.
    """
    from ..nvfp4.nvfp4_hadamard import build_hadamard, rotate_last_dim

    def forward_parity(self, input, *args, **kwargs):
        nv = bool(getattr(self, "_hswq_nvfp4_convrot", False))
        i8 = bool(getattr(self, "_hswq_int8_convrot", False))
        if nv or i8:
            if nv:
                gs = int(getattr(self, "_hswq_nvfp4_convrot_groupsize", 256) or 256)
            else:
                gs = int(getattr(self, "_hswq_int8_convrot_groupsize", 256) or 256)
            h = getattr(self, "_hswq_nvfp4_parity_H", None)
            need_rebuild = True
            if h is not None:
                try:
                    from ..nvfp4.nvfp4_hadamard import _tensor_storage_ok

                    # Global cache already rejects poisoned H via
                    # _tensor_storage_ok; module-local H must use the same
                    # check. nbytes==0 alone misses emptied shells that still
                    # report device/dtype (Distorch Method 3), so 2nd+ gen
                    # rotates with garbage and quality decays.
                    need_rebuild = (
                        h.device != input.device
                        or h.dtype != input.dtype
                        or (bool(input.is_cuda) and not bool(h.is_cuda))
                        or not _tensor_storage_ok(h)
                    )
                except Exception:
                    need_rebuild = True
            if need_rebuild:
                h = build_hadamard(gs, device=input.device, dtype=input.dtype)
                self._hswq_nvfp4_parity_H = h
            input = rotate_last_dim(input, h, gs)
        return stock_forward(self, input, *args, **kwargs)

    forward_parity._hswq_nvfp4_convrot_parity = True  # type: ignore[attr-defined]
    return forward_parity
```

### 3.4 同一 parity 文件内相关 docstring / 注释拼写（无行为变更）

`clear_nvfp4_parity_hadamard_caches` 的 docstring 与注释现为 **Distorch**（不再写 `DistOrch`）。`_unwrap_stock_forward` 的 docstring 同理。函数本体未改。

---

## ④ 含义

| 片段 | 含义 |
|------|------|
| `_tensor_storage_ok(t)` | 若 `t` 缺失、为空（`numel` / `nbytes`）、不是合法正方 Hadamard shape、或探测时抛错，则返回 False——即 Distorch Method 3 已清空 / 破坏 storage，但 Python 引用仍在的情况。 |
| 全局 `build_hadamard`（原本已有） | 不会返回毒化的 cache 条目；`_tensor_storage_ok` 失败时重建。 |
| 旧模块门控 | 在 device/dtype 看起来正常且 `nbytes != 0` 时仍可能保留 `_hswq_nvfp4_parity_H`，即使按 `_tensor_storage_ok` 仍不安全 → purge 后 **第 2 次及之后**用垃圾 `H` 做 `rotate_last_dim`。 |
| 新 `need_rebuild = … or not _tensor_storage_ok(h)` | 模块本地复用与全局 cache 使用**同一**存活判定。storage 毒化则经 `build_hadamard` 重建并写回新的 `_hswq_nvfp4_parity_H`。 |
| 删除单独的 `numel==0` / 后置 `nbytes==0` 分支 | 已由 `_tensor_storage_ok` 覆盖（`numel`、`nbytes` 与 shape）。避免再维护一套比全局更薄、会分叉的策略。 |
| `rotate_last_dim(input, h, gs)` | 契约不变：NVFP4 / INT8 protect ConvRot 的在线 act rotate。改变的只是**信任哪一个 `h`**。 |

### 操作说明

1. 更新到标签 **v3.3.4**。
2. 完全重启 ComfyUI。
3. 使用 HSWQ NVFP4 / INT8 时，在工作流末尾保持 **General Purge VRAM V2**（**`HSWQ`** 开启）。
4. 确认 purge 后第 2 次（及之后）生成不再因毒化的模块本地 Hadamard 复用而劣化。

### 兼容性

| 项目 | 策略 |
|------|------|
| 范围 | **HSWQ ConvRot INT8/ConvRot NVFP4 UNet Loader** 上的 Z Image / ZIT **ConvRot NVFP4** / INT8 protect 在线 rotate |
| 量化器 | **仅** [Hybrid-Sensitivity-Weighted-Quantization](https://github.com/ussoewwin/Hybrid-Sensitivity-Weighted-Quantization) |
| SDXL ConvRot NVFP4 | 不变（Checkpoint Loader + `nodes/nvfp4` TC 产品路径） |
| ComfyUI-master | 本扩展不修改 |
